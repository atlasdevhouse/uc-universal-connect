// For Nodemailer to function correctly within Next.js API routes, it relies on Node.js specific modules like crypto.
// This ensures that the module is only included in the server-side bundle.
"use server";
